spring:
  datasource:
    url: ${MARKET_DB_URL}
    username: ${MARKET_DB_USERNAME}
    password: ${MARKET_DB_PASSWORD}
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}
admin:
  username: ${ADMIN_USERNAME}
  password: ${ADMIN_PASSWORD}